import { Metadata } from "next";

export const metadata : Metadata = {
    title: 'Customers',
    description: 'Page for browse all customers? who use this application',
}

export default function Page() {
    return <p>Customers Page</p>;
}